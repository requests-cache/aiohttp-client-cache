# Read the Docs configuration file
version: 2

sphinx:
  builder: html
  configuration: docs/conf.py

python:
  version: 3.10
  install:
    - method: pip
      path: .
      extra_requirements:
        - all
        - docs
