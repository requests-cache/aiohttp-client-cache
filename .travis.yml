language: python
sudo: false
python:
  - "3.4"
  - "3.5"

services:
  - mongodb
  - redis-server

install:
  - pip install --upgrade .
  - pip install --upgrade -r requirements-test.txt
  - pip install coveralls==1.1

script: py.test -s -v tests --cov aiohttp_client_cache

after_success:
  coveralls
